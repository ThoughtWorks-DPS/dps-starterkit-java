/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user manual at https://docs.gradle.org/6.7/userguide/multi_project_builds.html
 */


pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
        jcenter()
        maven { url "https://palantir.bintray.com/releases" }
        maven { url  "https://dl.bintray.com/adesso/junit-insights" }
        maven {
            name = "starterBootPkgs"
            url = uri("https://maven.pkg.github.com/ThoughtWorks-DPS/dps-starter-boot")
            credentials(HttpHeaderCredentials) {
                name = "Authorization"
                def result = System.getenv("GITHUB_ACCESS_TOKEN")
                def token = !result ? "GITHUB_ACCESS_TOKEN_FIXME" : result;
                value = "Bearer " + token
            }
            authentication {
                register("header", HttpHeaderAuthentication)
            }
        }
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
        maven {
            name = "starterBootPkgs"
            url = uri("https://maven.pkg.github.com/ThoughtWorks-DPS/dps-starter-boot")
            credentials(HttpHeaderCredentials) {
                name = "Authorization"
                def result = System.getenv("GITHUB_ACCESS_TOKEN")
                def token = !result ? "GITHUB_ACCESS_TOKEN_FIXME" : result;
                value = "Bearer " + token
            }
            authentication {
                register("header", HttpHeaderAuthentication)
            }
        }
    }
}


rootProject.name = '{{cookiecutter.PACKAGE_NAME}}'
include ':api'
include ':app'
include ':container-init'
include ':controller'
include ':data'
include ':db-init'
include ':opa-init'
include ':persistence:impl'
include ':persistence:model'
include ':scripts'
include ':service:activator'
include ':service:provider'
include ':service:spi'
